# Planner context — example

Copy this to `$DOPPELGANGER_HOME/agents/planner/context.md` for private knowledge that
only the planner needs. Injected opt-in into the planner's prompt; no file → skipped.
Shared facts (family decoder, who you are) belong in `soul.md`, not here.

## Calendar habits

- Recurring patterns, default locations, how to read ambiguous event titles, etc.
