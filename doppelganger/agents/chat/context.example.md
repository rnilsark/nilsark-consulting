# Chat context — example

Copy this to `$DOPPELGANGER_HOME/agents/chat/context.md` for private knowledge that helps chat
reply well. Injected opt-in; no file → skipped. Shared facts (who's who, family decoder) belong in
`soul.md`, not here.

## Tone & habits

- How the harness should sound in the thread (warm, terse, emoji or not).
- Standing preferences ("always confirm bookings with a 👍", default meeting length, etc.).
