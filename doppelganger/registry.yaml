# Source of truth: which agents exist and who may call them.
# The dispatcher validates queue rows against this file; relevant parts
# are injected into the agent's prompt. `tools` becomes --allowedTools and
# `model` becomes --model for claude -p (omit model → CLI default).

# The DUTIES the doppelgänger carries on the operator's behalf — each is a role the operator would
# otherwise fill themselves. A duty OWNS a cluster of agents (the constellation renders each duty as a
# star-formation). Agents NOT listed under any duty are harness-general — comms (triage, chat) and the
# ingress sources — which serve every duty and belong to none, though they may talk to a duty's agents.
duties:
  entrepreneur:
    summary: The business-owner duty — collect, classify, reconcile and report the company's finances.
    agents: [inbox, intake, classifier, statement, reconciler, digest]
  calendar:
    summary: The scheduling duty — keep and answer the operator's calendar.
    agents: [planner]

agents:
  # Cheap, fast gate over incoming chat. Knows the harness's identity (call sign) and
  # capabilities; decides only whether a message is directed at the harness. Acts on nothing —
  # it can only order `chat`. No domain tools.
  triage:
    can_be_called_by: [chat-ingest]   # top-level (parent=null); documents intent
    tools: "Read,Write"               # Write is only for the out.json contract — no domain access
    model: haiku

  # The I/O security principal for untrusted external text. Composes the reply and delegates
  # domain work as orders. Holds NO domain credentials (no gws/Fortnox) — it can only talk and
  # delegate. Capable model: chat is where intent is understood and turned into action.
  chat:
    can_be_called_by: [triage]
    tools: "Read,Write"
    model: opus

  # Cheap, fast gate over incoming finance email (mirrors `triage` for the inbox path). Sees only
  # untrusted message METADATA ({messageId, from, subject, snippet, attachments}) — never attachment
  # bytes. Decides one thing: is this a bank statement (→ statement) or a document (→ intake), then
  # orders that scoped TS orchestrator to fetch + process that ONE message. Acts on nothing itself —
  # no domain tools, can ONLY order `intake` or `statement`.
  inbox:
    can_be_called_by: [inbox-ingest]  # top-level (parent=null); documents intent
    tools: "Read,Write"               # Write is only for the out.json contract — no domain access
    model: haiku

  # Calendar agent. Scoped to gws calendar only — even an injected order can't reach
  # gmail/drive/Fortnox. Reachable from the schedule (morning brief) and from chat (delegation).
  planner:
    can_be_called_by: [schedule, chat]
    tools: "Bash(gws calendar:*),Bash(date:*),Read,Write"
    model: sonnet

  # (The credentialed `entrepreneur` LLM agent was dissolved — every step it did is now deterministic
  # TS: collection via intake/statement, the heartbeat via the `digest` orchestrator below. There is
  # no monolithic finance LLM anymore; the only finance judgment left lives in the two kernels.)

  # First judgment kernel of the finance domain. Stateless: given ONE already-downloaded document,
  # classify it + extract its fields and return them as the out.json `result`. No Drive, no Gmail, no
  # state — pure judgment, so it's cheap, isolated, and independently testable. `intake` dispatches it.
  classifier:
    can_be_called_by: [intake]
    tools: "Read,Write,Skill"          # Read the file, Skill for the invoice tools, Write the out.json
    model: sonnet

  # The finance intake orchestrator (the entrepreneur decomposition, live). NOT an LLM: the worker runs
  # `runIntake` in TS for this agent — download the attachment(s), dispatch `classifier` for judgment,
  # normalize + file to Drive + state.md + state.json. Ordered by the `inbox` router for documents, in
  # place of a full entrepreneur run. No tools/model: it's code, not a claude run.
  intake:
    can_be_called_by: [inbox]
    tools: ""
    kind: orchestrator

  # Second judgment kernel: match a bank statement against the month's unpaid invoices. Stateless —
  # given the statement file + the invoice list, return each transaction with its matched invoice +
  # confidence. No Drive/Gmail/state; the `statement` orchestrator applies the matches.
  reconciler:
    can_be_called_by: [statement]
    tools: "Read,Write,Skill"
    model: sonnet

  # The bank-statement orchestrator (TS, not an LLM): the worker runs runReconcile — read the month's
  # unpaid invoices, dispatch `reconciler` for the matching, apply (mark paid + record txns) to
  # state.md + state.json. Triggered by the `inbox` router (a statement emailed in) or the Drive
  # drop-folder poll (a statement uploaded straight to Drive). Named for its input, not its kernel.
  statement:
    can_be_called_by: [inbox]
    tools: ""
    kind: orchestrator

  # The finance heartbeat orchestrator (TS, not an LLM): the worker runs the rollup — mark overdue,
  # recompute notify.items + the actionable-set fingerprint, scan anomalies, compose + push the todo,
  # and close the first ready prior month as Gmail DRAFTS. It digests the month's state into one todo;
  # all of it is deterministic procedure, so there is no model/tools — the worker executes TS. Fired
  # by the skip-gate (the heartbeat cron) and, for a forced month / ack, by chat.
  digest:
    can_be_called_by: [schedule, chat]
    tools: ""
    kind: orchestrator
    max_concurrency: 1   # never two rollups writing state.json at once (a chat-forced run vs the cron)
